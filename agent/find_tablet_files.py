import os
import win32com.client # In case pywin32 is installed, but we can also use PowerShell shell.application

# Let's write a PowerShell command to scan the shell namespace for "T-Mobile REVVL TAB" and find any files.
